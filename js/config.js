/**
 * SilentFlare - Global Configuration
 */

export const GITHUB_CONFIG = {
    // Your GitHub username
    username: 'silentflarecom',
    // Your repository name (usually same as username.github.io or custom domain)
    repo: 'SilentFlare.com',
    // Branch name
    branch: 'main',
};
